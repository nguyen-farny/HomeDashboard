<Window x:Class="DashBoardWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DashBoardWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Label x:Name="label" Content="Departure "/>
        <!--<DataGrid Name="rer_grid" HorizontalAlignment="Left" Margin="28,23,0,0" 
                 AutoGenerateColumns="True" VerticalAlignment="Top" Height="185" Width="451">
        </DataGrid>-->
        <DataGrid Name="rer_grid" HorizontalAlignment="Left" Margin="28,23,0,0" 
                 AutoGenerateColumns="false" VerticalAlignment="Top" Height="185" Width="451">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Schedules" Width="80" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Path=message}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
